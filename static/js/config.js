/* ВОнлайне — настройки сайта. Правятся прямо здесь, руками, обычным блокнотом.
   Это единственный файл, который нужно трогать при заливке. */
window.VO_CONFIG = {

  /* Связь с сетью RetroCore.
     false — сайт живёт сам по себе: никуда не ходит, ничего о сети не знает,
             раздел «Люди сети» скрыт, хозяин включает правку ссылкой внизу;
     true  — сайт подключает скрипт сети и знает, кто его открыл: здоровается
             по имени, показывает участников, сам узнаёт хозяина страницы
             и шлёт показатели в карточку его профиля.
     Включать имеет смысл, только когда сайт загружен в саму сеть (/sites/…). */
  retrocore: false,

  /* Откуда берётся скрипт сети. Менять незачем, пока сайт лежит в RetroCore. */
  retrocore_script: '/connect.js',

  /* Название сайта в шапке и в заголовках вкладок. */
  title: 'ВОнлайне',
};
