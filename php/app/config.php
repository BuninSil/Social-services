<?php
/**
 * ВОнлайне — настройки сайта.
 *
 * Это единственный файл, который нужно править при заливке на хостинг.
 * Остальное работает как есть.
 */

return [

    /* ------------------------------------------------------------- сеть RetroCore */

    // Тумблер. false — сайт живёт сам по себе: свои логины, свои профили,
    // наружу не ходит ни одного запроса. true — участники входят аккаунтом
    // сети RetroCore, оттуда же берутся их имена, аватарки и список людей.
    // Всё остальное — записи, сообщения, фотографии — в любом случае лежит
    // здесь, в папках сайта.
    'retrocore' => false,

    // Адрес сети и данные приложения из панели: Панель сервера → Приложения.
    // Адрес возврата должен совпадать символ в символ с указанным там.
    'retrocore_base'          => 'http://88.87.70.78:8080',
    'retrocore_client_id'     => '',
    'retrocore_client_secret' => '',
    'retrocore_redirect_uri'  => '',

    /* ------------------------------------------------------------------ сайт */

    'title' => 'ВОнлайне',

    // Регистрация для всех. Выключите, когда позовёте всех, кого хотели:
    // тогда новые страницы заводятся только командой php tools/account.php.
    'open_register' => true,

    // Оформление по умолчанию: vo | dark | neon | fresh | modern
    'theme' => 'vo',

    /* ----------------------------------------------------------------- файлы */

    // Пределы на загрузку, в мегабайтах. Хостинг может резать сильнее —
    // смотрите upload_max_filesize и post_max_size в его настройках.
    'limit_image' => 12,
    'limit_audio' => 30,
    'limit_video' => 256,
    'limit_doc'   => 50,

    // Уменьшать картинки на сервере (нужен модуль gd). Если его нет,
    // фотографии сохраняются как есть.
    'resize_images' => true,
];
